[project]
name = "rag-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chintan-solanki", email = "chinsol.8973@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "arxiv>=4.0.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[project.scripts]
rag-demo = "rag_demo:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
